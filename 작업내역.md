# 작업 내역

작성일: 2026-05-12  
브랜치: `main`

## 목표

명판 이미지, PDF, 자유 텍스트를 입력받아 표준 후보 검색과 LLM 보조 판단을 거쳐 AAS JSON을 생성하는 현재 아키텍처로 프로젝트를 정리했다.

## 최종 반영 구조

```text
Input
→ DefaultInputLayer
→ DocumentProcessor
→ ManualInputExtractor 또는 LLMExtractor
→ LLMSemanticNodeBuilder + ValueNormalizer
→ HybridStandardsCandidateRetriever
→ LLMMatcher.match_candidates
→ Best Match Selection
→ TemplateAwareAASMapper
→ JsonAASGenerator
→ DT registration/validation
```

## 주요 변경

| 영역 | 변경 |
|---|---|
| 입력 | 이미지/PDF 외에 `text`, `input_text`, `free_text`, `manual_text`, `user_text`, `raw_text` 지원 |
| 텍스트 추출 | PDF `pdfplumber`, 이미지 `easyocr`, LLM text cleaning 경로 유지 |
| Semantic Node | LLM enrichment와 ECLASS alias → `eclass_irdi` 보강 |
| 후보 검색 | Submodel Templates, ECLASS, IEC CDD 통합 검색 |
| 재랭킹 | `BaseEntityMatcher.match_candidates()` 추가, LLM batch reranking 지원 |
| 매핑 | `MatchedProperty`에 source/path/template/eclass 식별자 보존 |
| Submodel 배치 | `TemplateAwareAASMapper`에 deterministic selector와 LLM Submodel Template selector 추가 |
| AAS 생성 | ECLASS IRDI를 `supplementalSemanticIds`와 ConceptDescription에 반영 |
| 실행 모드 | `default`, `llm`, `yolo`, `llm-yolo` factory 정리 |

## Submodel 배치 정책

`TemplateAwareAASMapper`는 선택된 property를 그대로 배치하지 않고 다음 순서로 검증한다.

```text
MatchedProperty
→ Submodel 후보 생성
   - 현재 후보의 submodel
   - semanticId/eclassIrdi/idShort가 존재하는 template submodel
   - DigitalNameplate/TechnicalData/ProvisionOf3DModels/OperationalData heuristic
→ default: deterministic selector
→ llm: Ollama LLM selector
→ confidence/reviewRequired/diagnostics 기록
```

실제 확인 중 `NominalVoltage`가 `DigitalBatteryPassport`로 끌리는 오배치 사례가 있었고, core Submodel 우선순위와 technical keyword heuristic을 보강해 현재는 `TechnicalData`에 배치된다.

## 표준 데이터 반영

- `repositories/submodel_templates/admin_shell_io_submodel_templates/`
  - admin-shell-io Submodel Templates `main` snapshot
  - `published/` 아래 JSON 템플릿 151개 기준
- `repositories/eclass_dictionary/eclass_properties.json`
  - ECLASS alias/IRDI seed
- `repositories/iec_cdd_dictionary/iec_cdd_properties.json`
  - IEC CDD seed

## 검증

```bash
python3 -m unittest
```

결과:

```text
Ran 32 tests
OK
```

샘플 default pipeline 배치 결과:

```text
DigitalNameplate:
- AssetName
- ManufacturerName
- ManufacturerProductDesignation
- SerialNumber

TechnicalData:
- AssetType
- NominalVoltage
- RatedCurrent
- Weight

ProvisionOf3DModels:
- ModelFile
- ModelFormat
- GenerationMethod
```

## 남은 주의점

- Ollama가 실행 중일 때만 LLM extraction, embedding, batch reranking, LLM Submodel placement가 활성화된다.
- `pdfplumber`, `easyocr`, `ultralytics`는 선택 의존성이다. 설치되지 않으면 해당 단계는 fallback 또는 빈 결과가 된다.
- 최종 AAS validation은 lightweight 검사다. 운영 단계에서는 AAS 공식 JSON Schema 또는 BaSyx 기반 검증으로 확장하는 것이 좋다.

---

## 추가 작업 (2026-05-12)

### OCR 노이즈 가비지 필터 추가 (`modules/extraction/llm_extractor.py`)

OCR 입력 시 오추출되는 값을 제거하는 필터 5종 추가:

| 필터 | 설명 |
|---|---|
| 괄호 단독값 | `[Mbps]`처럼 괄호만으로 구성된 값 제거 |
| 숫자 부재 | 숫자가 없는 값(단위만 추출) 제거 |
| 인코더 해상도 | 단위 없는 Dynamixel 해상도 값(1024·2048·4096·1008·512·256) 제거 |
| 치수-질량 모순 | Width/Height 등에 g·kg 단위가 붙은 경우 제거 |
| 신뢰도 임계값 | confidence < 0.60 제거 |

### ECLASS IRDI 결정론적 매칭 구현

- `repositories/eclass_dictionary/eclass_properties.json` 신규 생성: ECLASS 13.0 표준 IRDI 사전
- `app/models.py`: `SemanticNode`, `AASPropertyCandidate`에 `eclass_irdi` 필드 추가
- `modules/semantic_node/llm_semantic_builder.py`: ECLASS 사전 조회 → IRDI 할당
- `modules/retrieval/embedding_retriever.py`: IRDI 정확 매칭 1순위 처리 (score=1.0, 임베딩 스킵)
- `repositories/aas_property_repository/properties.json`: 각 항목에 `eclass_irdi` 필드 추가

매칭 우선순위:
```
1순위: ECLASS IRDI 정확 매칭 → score=1.0
2순위: 임베딩 코사인 유사도 + alias 부스트 (+0.30 완전일치 / +0.12 부분일치)
3순위: LLM 정밀 매핑 (fallback)
```

### IDTA 공식 템플릿 기반 IRDI 사전 확장

- IDTA 공식 GitHub(Digital Nameplate 3/0, TechnicalData 1/2)에서 공식 IRDI 수집
- `eclass_properties.json`: 수작업 12개 → 공식 템플릿 기반 **21개** 속성으로 확장
- 기존 IRDI 8개를 최신 공식 버전으로 업데이트 (예: ManufacturerName `#002` → `#004`)
- 한국어 alias 추가 (제조사, 시리얼번호, 제품명 등)

### 검증 결과 (3종 산업용 로봇)

| 자산 | F1 Score | 서브모델 분류 |
|---|---|---|
| OMY-Pro | 0.947 | 100% |
| ROBOTIS Hand E1-I | 1.000 | 100% |
| OMX-M6 | 0.889 | 100% |
| **평균** | **0.930** | **100%** |

처리 시간: 수작업 35분 → 자동화 약 114초 (94.6% 단축)
