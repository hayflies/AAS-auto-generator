# AAS Auto Generator — 작업 내역

작성일: 2026-05-11  
담당자: minjae  
브랜치: `main`

---

## 작업 개요

AAS 자동 생성 파이프라인 LLM 파트 구현 + 실제 로봇 자산 검증 + 웹 UI 개발

---

## 구현 모듈 (전체)

| 단계 | 모듈 | 파일 |
|---|---|---|
| 입력 | OCR + PDF 파서 | `modules/input_layer/document_processor.py` |
| 추출 | LLM 속성 추출기 | `modules/extraction/llm_extractor.py` |
| 의미 변환 | Semantic Node 빌더 | `modules/semantic_node/llm_semantic_builder.py` |
| 후보 검색 | 임베딩 기반 검색기 | `modules/retrieval/embedding_retriever.py` |
| 매칭 | LLM 매처 | `modules/matching/llm_matcher.py` |
| 매핑 | 서브모델 분류 + AAS 매핑 | `modules/aas_mapping/semantic_mapper.py` |
| 생성 | AAS JSON 생성기 | `modules/aas_generation/json_generator.py` |
| 웹 UI | FastAPI 백엔드 | `api.py`, `db.py` |
| 웹 UI | 프론트엔드 | `static/index.html` |

---

## 주요 버그 수정 이력

| 버그 | 원인 | 수정 파일 |
|---|---|---|
| `slugify("ROBOTIS")` → `"r_o_b_o_t_i_s"` | CamelCase 정규식이 대문자 연속 분해 | `app/text.py` — 소문자 변환 후 토크나이즈 |
| `Payload` / `Repeatability`가 DigitalNameplate에 배치됨 | sentence-transformers 재분류가 properties.json 서브모델 덮어씀 | `modules/aas_mapping/semantic_mapper.py` — properties.json 서브모델 우선 적용 |
| `ManufacturerName` → `ReferencedObjectType` 잘못 매핑 | 임베딩 유사도 노이즈 | `modules/retrieval/embedding_retriever.py` — alias 정확 일치 +0.30, 부분 일치 +0.12 부스트 |
| `CommunicationBaudrate` 값 = `[Mbps]` | OCR이 단위만 추출 | `modules/extraction/llm_extractor.py` — 브래킷 전용값 필터 |
| `JointRange` = `1008` | Dynamixel 엔코더 분해능이 각도로 오인 | `modules/extraction/llm_extractor.py` — 엔코더 분해능 블랙리스트 |
| `Width = 250g/mm` (FP) | 페이로드 수치가 치수로 오분류 | `modules/extraction/llm_extractor.py` — 치수+무게단위 조합 필터 |
| `SerialNumber: SN-001` 환각 | LLM이 프롬프트 예시값을 복사 | `modules/llm/prompts.py` — 예시값 변경 |
| `TypeError: float(None)` | LLM이 `score: null` 반환 | `modules/matching/llm_matcher.py` — `or 0.0` 처리 |
| `ReferencedObjectType`, `DocumentType` 노이즈 속성 AAS 포함 | 매칭 점수 임계값 너무 낮음 | `modules/aas_mapping/semantic_mapper.py` — `_MIN_SCORE` 0.68 → 0.70, `HandoverDocumentation` 제외 |

---

## properties.json 확장

로봇 자산 특화 속성 13개 추가 (총 81개):

`DegreesOfFreedom`, `Payload`, `MaxReach`, `Repeatability`, `MaxTCPSpeed`,
`HostInterface`, `CommunicationBaudrate`, `NumberOfJoints`, `JointRange`,
`OperatingVoltage`, `MaxJointTorque`, `RobotType`, `ProtectionClass`

---

## 성능 최적화

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 처리 시간 (103속성 기준) | 20~30분 | 수 초 |
| 방법 | 속성마다 LLM enrichment 호출 | `skip_enrichment=True` — LLM 없이 고정 문자열 사용 |
| 적용 위치 | `app/pipeline.py` | `LLMSemanticNodeBuilder(skip_enrichment=True)` |

---

## 웹 UI

- FastAPI + SQLite 기반
- 파일 드래그&드롭 업로드 (PDF, PNG, JPG 등)
- Asset Name / Manufacturer / Asset Type 입력
- 결과 목록 테이블 + AAS JSON 뷰어
- Copy JSON / Delete 기능
- Asset Name 미입력 시 유효성 검증

실행:
```bash
python -m uvicorn api:app --reload
# → http://localhost:8000
```

---

## 실제 디바이스 검증 결과

| 디바이스 | 입력 | F1 (속성명) | 값 정확도 | 비고 |
|---|---|---|---|---|
| OMY-Pro (6축 로봇암) | PDF 데이터시트 | 0.947 | 8/9 (89%) | SerialNumber 미기재 FN 1건 |
| ROBOTIS Hand (그리퍼) | 인터넷 캡처 | 1.000 | 4/4 (100%) | |
| OMX (5축 로봇암) | 인터넷 캡처 | 0.889 | 3/8 (37%) | OCR 노이즈, 코드 수정으로 일부 개선 |
| **평균** | | **0.945** | **15/21 (71%)** | |

서브모델 분류 정확도: 3개 디바이스 모두 **100%** (IDTA 표준 준수)  
처리 시간: 수작업 35분 → 자동화 평균 **1.9분** (94.6% 절감)

---

## 평가 자료

`paper_data/evaluation_results_v3.xlsx`
- 속성 추출 정확도 (3개 디바이스 비교)
- OMY-Pro / OMX 속성별 상세 결과
- 처리 시간 비교
- 서브모델 분류 정확도
- 종합 대시보드 (KPI)

---

## 주요 설치 요구사항

```bash
ollama pull llama3.2
ollama pull nomic-embed-text
pip install -r requirements.txt
```
